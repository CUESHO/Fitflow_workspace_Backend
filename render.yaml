# =====================================================================
# Blueprint de Render para el backend de FitFlow (Django REST Framework)
#
# Render lee este archivo desde la raíz del repositorio y crea:
#   1. Una base de datos PostgreSQL gratuita.
#   2. Un servicio web que ejecuta Django con gunicorn.
#
# El frontend de Angular vive en otro repositorio y se publica aparte
# como "Static Site" (ver README_DESPLIEGUE.md).
# =====================================================================

databases:
  - name: fitflow-db
    databaseName: fitflow
    user: fitflow
    plan: free

services:
  - type: web
    name: fitflow-backend
    runtime: python
    plan: free
    # El proyecto de Django no está en la raíz del repo, sino en /backend
    rootDir: backend
    buildCommand: "chmod +x build.sh && ./build.sh"
    startCommand: "gunicorn fitflow_api.wsgi:application"
    envVars:
      # Render inyecta aquí la cadena de conexión de la base de datos de arriba.
      - key: DATABASE_URL
        fromDatabase:
          name: fitflow-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ENV
        value: prod
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.12.7"
      # URL del sitio estático de Angular desplegado en Render.
      # Si cambia, hay que editarla aquí y no en el panel: el blueprint se
      # sincroniza solo y sobrescribiría cualquier cambio hecho a mano.
      - key: ALLOWED_ORIGINS
        value: "https://fitflow-frontend-c59m.onrender.com"
      # Datos del administrador. "sync: false" hace que Render te los pida al
      # desplegar, de modo que la contraseña nunca queda escrita en el
      # repositorio. Los usa build.sh para crear el superusuario.
      - key: DJANGO_SUPERUSER_USERNAME
        sync: false
      - key: DJANGO_SUPERUSER_EMAIL
        sync: false
      - key: DJANGO_SUPERUSER_PASSWORD
        sync: false
      # Carga datos de ejemplo al desplegar. El comando usa get_or_create,
      # así que puede ejecutarse en cada build sin duplicar registros.
      - key: SEED_DEMO
        value: "True"
